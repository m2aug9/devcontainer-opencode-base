services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/workspaces/ProjectName:cached
      - /home/ubuntu/.aws:/home/ubuntu/.aws
      - /home/ubuntu/.ssh:/home/ubuntu/.ssh
      - shell-history:/home/ubuntu/.history
    command: sleep infinity

volumes:
  shell-history:
